node_modules
frontend/dist
frontend/coverage
frontend/test-results
frontend/playwright-report

# --- Security ---
.env
.env.local
secret.json
/imp_config.json
/session.json
/config.json
/cli.json

# --- Wails v3 / Build Artifacts ---
# NOTE: frontend/bindings/ is intentionally NOT ignored - the generated
# TS bindings are committed, same as wailsjs/ was under Wails v2.
build/bin/
# `wails3 build` (used internally by `wails3 dev`, distinct from our
# `wails3 task build` which respects BIN_DIR=build/bin) defaults here.
/bin/
.task/
frontend/dist/
# Transient Windows resource file generated (and removed) by windows:generate:syso
*.syso
# Mobile: generated per build/machine (overlays hold absolute paths). The host
# sources under build/ios and build/android are committed.
build/ios/xcode/
build/android/overlay.json
build/android/gen/
build/android/app/src/main/jniLibs/
build/android/app/build/
build/android/build/
build/android/.gradle/
build/android/local.properties

# Downloaded by windows:installer; release packaging output.
build/windows/installer/MicrosoftEdgeWebview2Setup.exe
dist/

# --- Dependencies ---
node_modules/

# --- macOS System Files ---
.DS_Store
.AppleDouble
.LSOverride

# --- Editor Settings ---
.vscode/
.idea/
*.swp
*.swo
