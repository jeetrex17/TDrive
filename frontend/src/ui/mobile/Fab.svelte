<script lang="ts">
    // The upload button is the host the upload menu portals into: FeatureLayer
    // renders UploadMenu (its trigger + menu) into #upload-menu-root, and
    // mobile.css restyles that trigger into the docked accent circle. The host
    // stays mounted so the portal never breaks.
</script>

<div id="upload-menu-root" class="mobile-fab"></div>
